{
  "name": "fakelish-example",
  "version": "1.0.0",
  "description": "Example of fakelish",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d dist",
    "examples": "npm run build && node dist/example1.js"
  },
  "author": "Ryo Ota",
  "dependencies": {
    "fakelish": "file:.."
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-es2016": "^6.24.1",
    "babel-preset-stage-3": "^6.24.1"
  }
}
